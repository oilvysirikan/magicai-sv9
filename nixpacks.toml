[phases.setup]
nixPkgs = ["php84", "php84Extensions.gd", "php84Extensions.pdo_mysql", 
           "php84Extensions.mbstring", "php84Extensions.zip", 
           "php84Extensions.redis", "php84Extensions.bcmath",
           "php84Extensions.pcntl", "composer"]

[start]
cmd = "php artisan octane:start --server=swoole --host=0.0.0.0 --port=$PORT"
