[phases.setup]
nixPkgs = ["php82", "php82Extensions.gd", "php82Extensions.pdo_mysql", 
           "php82Extensions.mbstring", "php82Extensions.zip", 
           "php82Extensions.redis", "php82Extensions.bcmath",
           "php82Extensions.pcntl", "composer"]

[start]
cmd = "php artisan octane:start --server=swoole --host=0.0.0.0 --port=$PORT"
